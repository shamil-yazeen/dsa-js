# Sliding Window Pattern

The Sliding Window pattern is used to reduce nested loops
when working with consecutive elements in arrays or strings.

Instead of recalculating values repeatedly,
we reuse previous computations by adjusting a window.

---

## Problems

### 1️⃣ max-subarray-sum
Find the maximum sum of consecutive elements of fixed size.