# Recursion Pattern

The Recursion pattern solves problems by having a function
call itself with smaller inputs until it reaches a base case.

It is useful when a problem can be broken into smaller,
similar subproblems.


### 1️⃣ power (exponentiation)
Compute base raised to a power using recursion.

Optimized using exponentiation by squaring.

